## 1 Redirection and Piping



Combine the contents of `students.txt` and `grades.txt` into a new file named `students_grades_combined.txt`. The content of `students.txt` should appear first, followed by the content of `grades.txt`.

![image]()

Append the current date and time to the end of the `students_grades_combined.txt` file.

![image](2.png)

Generate a file named `directory_listing.txt` containing the detailed listing of all files in the current working directory.

![image](3.png)

Append the detailed listing of the home directory (accessible via `/home/[username]` or using the tilde symbol `~`) to the existing `directory_listing.txt` file.

![image](4.png)

Take the contents of `students.txt`, sort them alphabetically, and save the sorted output to `sorted_students.txt`.

![image](5.png)

List all files in the current directory, filter only those containing “student” in the name, and count how many such files exist.

![image](6.png)

Take the first 5 lines of `students.txt` and then display only the last 2 lines of those 5 lines. (Hint: use `head` and `tail` with pipe)

![image](7.png)
